# avi-model-workshop

Landing page for the RC Atelier — deployed to GitHub Pages. Custom domain: `AtelierRC.mirkit.fr`.

This repository contains a single-page site (index.html). A GitHub Actions workflow in `.github/workflows/deploy-pages.yml` deploys the repository root to GitHub Pages on push to `main`.
